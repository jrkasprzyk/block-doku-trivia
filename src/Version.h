// Version.h — application version and build identifier.
#pragma once

#include <string_view>

namespace td {
inline constexpr std::string_view kAppVersion = "0.1.2";
inline constexpr std::string_view kBuildId    = "lego-set";
} // namespace td
